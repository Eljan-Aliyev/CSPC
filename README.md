## PW1 — Lab A: Radioactive Decay Simulation

### Results
- **Test Suite Status**: 3/3 passing (`pytest -v`)
- **Pure-Python Loop Execution Time**: <INSERT_YOUR_LOOP_TIME> seconds
- **NumPy Vectorized Execution Time**: <INSERT_YOUR_NUMPY_TIME> seconds
- **Speed-up Factor**: NumPy is <INSERT_YOUR_SPEEDUP>x faster

### Conclusion
Vectorized operations in NumPy eliminate explicit Python loop overhead by processing array elements directly in compiled C code, yielding a significant speed improvement for large particle simulations.# CSPC - Lab A
